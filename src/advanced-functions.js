// Cache function
function cache(func) {
    // do your magic here
}

// Object "ladder"
const ladder = {
    // do your magic here
};

// ApplyAll function
function applyAll(func, arg1, arg2) {
    // do your magic here
}

module.exports = {cache, ladder, applyAll}
